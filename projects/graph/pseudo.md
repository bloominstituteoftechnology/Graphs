<h2>Graph Pseudo</h2>

<h4>BFT</h4>

```
        #make a queue 
        #make a visited set
        #put starting vertex in the queue 
        #while the q isnt empty 
        ## dequeue 
        ##mark current as visited
        ### for all of dqd item's edges
        ## put them in the queue
```
<h4>DFT</h4>

```
        # make a stack
        # make a visited stack
        #put starting vertex in our stack 
        #while stack !empty
        ##pop off the top of the stack, its our current item
        #mark it as visited
        #for each current items edges
        #if not visited 
        #put them on the stack
```