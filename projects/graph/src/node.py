class Node:
    id = 0
    def __init__(self, x, y, radius):
        self.x = x
        self.y = y
        self.radius = radius
        self.radius = radius
        self.id = Node.id
        Node.id += 1
